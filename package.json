{
  "name": "fortis-prompt-3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "playwright:install": "npx playwright install",
    "playwright:run": "npx playwright test",
    "playwright:report": "npx playwright show-report",
    "playwright:run:headed": "npx playwright test --headed",
    "playwright:run:ui": "npx playwright test --ui",
    "playwright:run:debug": "npx playwright test --debug"
  },
  "keywords": [],
  "author": "Lucas Avila",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.8.1"
  }
}
